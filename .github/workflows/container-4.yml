name: Container-4
on: push

jobs:
  container-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: run a script
        uses: docker://alpine
        with:
          entrypoint: ./script.sh
          args: 'hello-world'